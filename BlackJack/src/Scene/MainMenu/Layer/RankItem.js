let RankItem = cc.Layer.extend({
    ctor: function (num, headImg, name, golds) {
        this._super();

        return true;
    },
    loadItem: function (num, headImg, name, golds) {
        // let rankItem = cc.
    }
});